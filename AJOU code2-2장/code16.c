#include <stdio.h>
int main()
{
	/*
	
	배열은 변수의 나열이다.
	변수라 함은 단일 변수를 의미한다. 정수형 변수라면 하나의 정수 값만을 가질 수 있다.
	반면 배열은 여러 변수를 가진다. 정수형 배열은 여러개의 정수 값을 가질 수 있다.
	
	*/

	int a = 1;
	//a는 하나의 값만을 가진다.

	int b[4] = { 1,2,3,4 };
	//배열은 여러개의 값을 가지고 위치를 참조하여 각 변수를 불러올 수 있다.
	//변수는 b[0]의 위치부터 차례대로 저장된다.
}